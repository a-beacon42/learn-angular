import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  standalone: true,
  template: '<p>Select the recipes route to test lazy loading.</p>',
})
export class HomeComponent {}
